
    /*
     * Complete the function below.
     */
    static boolean check_if_sum_possible(long[] arr, long k) {
        return helper(arr, k, 0, 0);
    }

    static boolean helper(long[] arr, long target, long sofar, int idx) {
        if(sofar == target && idx!=0) {
            return true;
        } else if(idx >= arr.length) {
            return false;
        } else {
            for(int i=idx; i<arr.length; i++) {
                //-ve ints present so wont work
                //if( (sofar + arr[i]) <= target) {
                    //include
                    //xplore
                    if(helper(arr, target, sofar+arr[i], i+1)) {
                        return true;
                    }
                //}
            }
            return false;
        }
    }

